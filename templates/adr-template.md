# Architecture Decision Record Template

Use an ADR for material repository or implementation decisions. Record the context, decision, consequences, considered alternatives, decider roles, date, and lifecycle status.

The machine-readable example is [`architecture-decision.yaml`](architecture-decision.yaml). Validate it with `gaso validate`.

An ADR explains a design decision; it does not grant operational authorization or accept risk on behalf of an unlisted authority.
